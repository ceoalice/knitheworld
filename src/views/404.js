import React from 'react';
import ReactDOM from 'react-dom';

ReactDOM.render(
    <React.StrictMode>
      <div>
        HELLO 404 dev server
      </div>
    </React.StrictMode>,
    document.getElementById('root')
);